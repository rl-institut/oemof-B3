def stack_timeseries(df):
    _df = df.copy()

    # assert that df has a timeindex

    # stack timeseries
    df_stacked = _df

    return df_stacked


def unstack_timeseries(df):
    _df = df.copy()

    # stack timeseries
    df_unstacked = _df

    return df_unstacked
